schema_version: '1.0'
task_id: P04-S02-T01
revision: 1
title: Implement checkpoint capture and baseline policies
type: implementation
objective: Capture HEAD, dirty state, diff, untracked paths, and enforce allow-and-preserve, require-clean,
  and allow-listed policies.
scope:
  include:
  - src/uads/checkpoints
  - tests/checkpoints
  exclude: []
dependencies:
- P02-S01-T02
inputs:
- SYSTEM_SPEC_V1_REV2.md
- IMPLEMENTATION_PLAN.md
- .uads/tasks/P02-S01-T02.yaml
constraints:
- Follow Revision 2 contracts and fail-closed validation.
- Do not modify the HR project.
- Do not start a later Phase before the current Stage gate passes.
acceptance_criteria:
- statement: user changes are never silently reset or attributed; policy is checked before routing.
  verification: automated_or_reviewed
  id: AC-01
required_capabilities:
- code_editing
- structured_output
- tool_use
risk: medium
complexity: medium
recommended_executor: local-process executor
recommended_model_profile: coding-reasoning
validation:
  gate: clean, dirty, untracked, and allow-list scenarios pass.
  completion_signal: baseline policy matrix passes.
autonomy:
  mode: stage-gated
expected_outputs:
- checkpoint artifact set and baseline decision.
retry_budget:
  task: 2
  stage: 4
  run: 1
scope_description: repository adapter, checkpoint artifacts, policy tests.
non_goals:
- provider-specific production integrations
- distributed queues
- UI
- HR project changes
allowed_changes:
  paths:
  - src/uads/checkpoints
  - tests/checkpoints
