schema_version: '1.0'
task_id: P04-S01-T02
revision: 1
title: Implement reproducible bundle identity
type: implementation
objective: Hash canonical bundle metadata and content references exactly as specified.
scope:
  include:
  - src/uads/context
  - tests/context
  exclude: []
dependencies:
- P04-S01-T01
inputs:
- SYSTEM_SPEC_V1_REV2.md
- IMPLEMENTATION_PLAN.md
- .uads/tasks/P04-S01-T01.yaml
constraints:
- Follow Revision 2 contracts and fail-closed validation.
- Do not modify the HR project.
- Do not start a later Phase before the current Stage gate passes.
acceptance_criteria:
- statement: same inputs reproduce the same identity; any source, order, builder-version, or content change
    changes it.
  verification: automated_or_reviewed
  id: AC-01
required_capabilities:
- code_editing
- structured_output
- tool_use
risk: medium
complexity: medium
recommended_executor: local-process executor
recommended_model_profile: coding-reasoning
validation:
  gate: hash vectors and mutation tests pass.
  completion_signal: reproducibility suite passes.
autonomy:
  mode: stage-gated
expected_outputs:
- bundle hash and identity verifier.
retry_budget:
  task: 2
  stage: 4
  run: 1
scope_description: canonical JSON/hash implementation and reproducibility tests.
non_goals:
- provider-specific production integrations
- distributed queues
- UI
- HR project changes
allowed_changes:
  paths:
  - src/uads/context
  - tests/context
