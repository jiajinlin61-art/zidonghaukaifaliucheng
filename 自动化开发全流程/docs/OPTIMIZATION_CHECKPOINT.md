# 流程优化检查点

更新时间：2026-09-24T15:37:29+08:00。当前状态：已同步指定目录并完成本地验收，无待执行的本轮开发步骤。

## 目标、真源与停止点

用户批准实施审查建议 1—4，并要求按需加载以减少 token；明确不做第 5 项无人值守调度。范围仅为 `E:\自动化开发任务通用流程\自动化开发全流程`，不改变模型绑定、UADS 规格/任务种子或相邻 GPT-TaskBoard。本轮停止于优化、验证与交接，不自动提交、发布或部署。

恢复时先读 [AGENTS.md](../AGENTS.md)，再读本文件；按当前问题打开对应工具/测试。业务执行入口是 [最小执行规则](../ai-development-workflow-v1/PROJECT_EXECUTION_RULES.md)，按需读取的唯一来源是 [上下文索引](../ai-development-workflow-v1/policies/CONTEXT_POLICY.md)。无需重读全库。

## 已核实状态

- 40 个新增/修改文件已同步；原目录同步前逐文件核对原始哈希，备份位于 `C:\Users\JJL\AppData\Local\Temp\workflow-opt-backup-20260924`。临时备份不是长期版本管理，可能被系统清理。
- Contract 1.4 已实现成功退出约束、真实命令采集、实际改动核对、日志完整性及 task/revision/attempt 绑定、Git/内容新鲜度、原子更新 Result、语义/人工报告绑定和项目验收覆盖检查。
- Git 成功交付要求已提交且工作区干净，基线到 HEAD 差异与采集结果一致；遇原有用户修改先隔离，工具不自动提交/清理。
- 三个常用入口正文字符总量从 8999 减至 2757（减少 69.4%）；这是字符计数，不是实际模型 token/费用测量。详细规则改按事件读取，starter 仍可独立复制使用。
- 原虚拟环境已通过项目原有构建流程重新安装可编辑包，`uads` 导入指向当前 `自动化开发全流程/src`。沿用原 Python 环境，未新增运行依赖。
- 项目父仓库：`E:\自动化开发任务通用流程`，分支 `main`，基线 `eb39b31596f98207e229e93fc522d742282e48f5`。本轮没有创建提交，交付保留在工作区（27 个已跟踪文件修改、13 个新增文件）。

## 当前验证证据

以下命令均在目标目录，使用其现有 `.venv/Scripts/python.exe` 执行：

| 检查 | 本轮结果 | 适用边界 |
|---|---|---|
| `-B tools/check_tests.py` | 114 项通过 | 最终目标目录；日志 `C:\Users\JJL\AppData\Local\Temp\workflow-opt-target-tests.log` |
| `-B tools/environment_check.py` | 通过 | Python、当前导入路径、依赖、Git 和工具存在性；不等于模型 probe |
| `-B tools/workflow_docs_validate.py` | 0 问题 | 入口长度、本地链接和已知过期审批条款 |
| `-B tools/bootstrap_validate.py` | 35 个任务、35 个依赖节点通过 | 规格种子未变更，不表示运行时完成 |
| `git diff --check` | 通过 | 工作区差异；Git 提示 LF/CRLF 归一化，不是内容错误 |

新增测试包括真实子进程成功/失败/超时、临时 Git 提交、日志篡改、旧 attempt、内容及提交变化、实际越界/漏报改动、未提交交付、Result 替换失败、完整 actual CLI 和项目验收映射。

## 独立审查与处理

使用 requesting-code-review 做一次独立只读审查。审查提出四项 Important：Git 未提交交付可能通过、任务检查缺少提交新鲜度、结果写入中断风险、报告跨 attempt 复用。均已修复并纳入主控执行的回归；没有未报告的重要发现。修复版本未做第二轮独立审查，114 项通过是主控实际测试证据。

## 重要决定与限制

1. Contract 升至 1.4；旧 1.3 结果不能仅改版本号冒充迁移，缺失证据须重新采集。review_revision 是审查轮次，task revision 由记录身份绑定。
2. Evidence 目录位于工作区外，采集工具只执行一个明确命令，不调度任务、不自动调用下一模型。
3. 本地哈希检查缺失/篡改/过期，不防能重写全部记录的恶意主体；业务语义和服务端实际模型身份仍依赖独立审查/预检。
4. 忽略的生成物/依赖环境、并发写工作区、脱离父进程的后台任务未获得完整验证；本流程使用受控串行任务，不宣称支持无人值守并发。
5. 需求映射通过不等于 Task 完成或项目 DONE，Controller 仍须完成集成和批准范围核对。
6. 本轮未运行真实付费模型开发链路、真实外部系统或部署，也未完成一个真实业务项目的全链路试用；本地进程/Git 测试不能替代这些证据。

## 关键规则核对与后续

用户“不做 5” → 未实现后台调度器，UADS 规格/种子保持原样。用户“按需加载” → 入口、场景索引与独立 starter 已落地，字符计数已核对。保护已有修改 → 同步前哈希校验并备份；无提交/回滚/清理用户内容。

本轮无阻塞或必须补做的开发步骤。后续实际使用从根 README/项目 AGENTS 进入；只有用户再授权真实项目演练或无人值守运行时开发，才展开这些新工作。不因本检查点存在自动启动新阶段。

## 2026-09-24 后续维护：多项目共享边界

用户授权：按已讨论方案修改模板、教程、共享使用边界；保留用户自行删除的 starter-project/src，不改 Windows ACL 或全局 Codex 配置。

- starter AGENTS 预置共享路径、待接入状态、版本/权限/证据目录待核验项；独立使用仍可用，首次明确启用后才执行完整规则包。
- 新增 policies/SHARED_LIBRARY_USAGE.md，按需索引可达；项目状态/Result 在项目内，原始证据按现有工具要求在代码工作区与共享库之外，项目/task/attempt 分开。
- 教程、首次提示词、README、证据调用示例同步。业务调用从业务 cwd 使用共享工具绝对路径，Python 禁写字节码缓存；共享库维护需单独授权。
- 版本记录包含 Manifest/契约、Git HEAD/dirty；对实际采用文件记录摘要，避免未提交修改未改变版本号时静默混用。摘要与版本记录不等于发布快照。
- 实际 CLI 演练发现 delivery_evidence.main 将 argparse 的 Path 对象随 vars(args) 写入初始 JSON，导致 TypeError。已改为只传任务身份字段，新增真实 CLI 两项目及成功/失败退出码回归。

本次实际验证：环境检查通过；完整 115 tests / 29.942s / OK；两个临时项目的命令行采集通过，共享 tools 文件和缓存哈希前后不变；本次 8 份文档的 22 个本地链接通过，用户 src 删除保留。演练记录：C:\Users\JJL\AppData\Local\Temp\workflow-project-isolation-uh0o_l43\verification.json。修改前备份：C:\Users\JJL\AppData\Local\Temp\workflow-shared-boundary-e_au5hcs（临时备份，不是发布版本）。

权限边界：当前会话的共享库位于默认可写根之外，本次维护通过授权提权写入。模板不能为其他项目设置托管沙箱权限；未来项目接入须核对实际权限，未核实只能标记规则约束。临时演练验证的是路径分离和无意外缓存写入，不是恶意写入阻断证明。未调用真实付费模型、未做业务全链路验收；本次没有新增后台调度器，没有提交 Git。
